import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF0A3D62); // Deep Blue
  static const Color primaryDark = Color(0xFF06253B);

  // Secondary
  static const Color secondary = Color(0xFF1DD1A1); // Teal Green
  static const Color secondaryDark = Color(0xFF108565);

  // Accent
  static const Color accent = Color(0xFFF2994A); // Warm Orange

  // Neutrals
  static const Color dark = Color(0xFF1E1E1E);
  static const Color light = Color(0xFFF9FAFB);
  static const Color grey = Color(0xFF9CA3AF);
  static const Color surface = Colors.white;
  static const Color background = Color(0xFFF3F4F6); // Light Grey Background

  // Status
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFF2C94C);
  static const Color error = Color(0xFFEB5757);
  static const Color info = Color(0xFF2F80ED);
}
